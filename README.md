# Лабораторная работа №30. JSON и модели данных

**ФИО:** Федоров Михаил Дмитриевич

**Группа:** ИСП-233

**Дата:** 10.05.2026

## Таблица всех маршрутов

|Маршрут|Описание|
|---|---|
|GET/api/Heroes|Получение всех героев и фильтрация по вселенной|
|GET/api/Heroes/{id}|Получение героя по id|
|GET/api/Heroes/demo|Показывает один объект дважды с разными настройками сериализатора|
|GET/api/Heroes/serialize|Отражает работу JsonSerializer напрямую|
|GET/api/Heroes/search|Маршрут поиска по имени|

## Таблица атрибутов JSON

|Атрибут / настройка|Что делает|Пример|
|---|---|---|
|[JsonPropertyName("name")]|Задаёт имя поля в JSON|"name" вместо "Name"|
|[JsonIgnore]|Поле не попадает в JSON|Пароли, внутренние поля|
|PropertyNamingPolicy.CamelCase|Все поля в camelCase|"realName" вместо "RealName"|
|JsonStringEnumConverter|Enum как строка|"Marvel" вместо 0|
|WriteIndented = true|JSON с отступами|Для удобства при разработке|

## Главные выводы

1. **Сериализация** преобразует C#-объект hero в строку JSON с учётом настроек options, в то время как **десериализация** действует обратно: берёт JSON-строку serialized и создаёт из неё новый 
объект типа Hero

2. Атрибут [JsonIgnore] полностью исключает свойство из сериализации, поле не попадёт в JSON-ответ клиенту

```
[JsonIgnore]
public string? InternalNotes {get; set; }
```

3. При помощи **JsonStringEnumConverter** enum сериализуется как строка. Это необходимо для сериализации данных перед их отправкой по сети

4. После настройки CamelCase мтроки будут преобразованы в формат `camelCase`

## Итоговая таблица: что изучили в лабораторной №30

|Концепция|Описание|
|---|---|
|JSON|Текстовый формат для передачи данных между сервером 
и клиентом|
|Сериализация| Объект C# → JSON-строка|
|Десериализация|JSON-строка → объект C#|
|[JsonPropertyName("name")]|Задать точное имя поля в JSON|
|[JsonIgnore]| Исключить поле из JSON|
|JsonNamingPolicy.CamelCase|Все поля автоматически в camelCase|
|JsonStringEnumConverter| Enum сериализуется как строка, не число|
|WriteIndented = true|JSON с отступами — читаемый вид|
|List<string>|В JSON становится массивом ["a","b","c"]|
|Вложенный объект|Класс внутри класса → объект внутри объекта в JSON|
|JsonSerializer.Serialize()|Ручная сериализация объекта в строку|
|JsonSerializer.Deserialize<T>()|Ручная десериализация строки в объект. <T> — тип, в 
который нужно преобразовать JSON. Например, 
Deserialize<Hero>(json) создаст объект типа Hero. <T> — 
это generics|